{
    "name": "parse-regex-properties",
    "version": "1.0.0",
    "description": "Fonctions utiles pour parser des regex custom des .properties en objet et vice versa",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "test": "jest",
        "prepublishOnly": "npm run build",
        "publish": "git add . && npm version patch && git push origin main && npm publish",
        "clean": "rm -rf dist"
    },
    "publishConfig": {
        "registry": "https://artifactory.s.arkea.com/artifactory/api/npm/npm/"
    },
    "author": "ns969",
    "license": "UNLICENSED",
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "@types/node": "^22.15.29",
        "jest": "^29.7.0",
        "ts-jest": "^29.3.4",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "zod": "^3.25.36"
    }
}
